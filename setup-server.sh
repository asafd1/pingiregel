# 1. install node
# 2. install mongo
# 3. generate key pair (see script)
# 4. init db (see script)
curl -sL https://deb.nodesource.com/setup_10.x | sudo bash -
sudo apt-get -y install nodejs

sudo apt install -y mongodb
sudo systemctl status mongodb

